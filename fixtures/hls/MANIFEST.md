# `fixtures/hls/` — provenance manifest

Every fixture in this tree is listed below with its source, when it was
added, its licence/redistribution status, and what it is *for*. If a fixture
starts failing and this manifest can't explain where it came from, that's a
bug in this file — fix it before touching the fixture.

Parsed and asserted by `transmux/tests/hls_fixture_corpus.rs` (`cargo test -p
transmux --test hls_fixture_corpus`). See that file's module doc for the
current pass/fail table — this file is provenance, that file is behaviour.

---

## Tier 0 — pre-existing hand-made fixtures (`fixtures/hls/*.m3u8`)

Added before issue #869; **hand-made bytes**, not derived from any real
capture or spec text. Adequate for negative unit tests (each is built to
trip one specific rule) but not spec-conformance evidence — that's what
Tier 1/2 below are for. No external source URL; authored in-repo.

| File | Purpose |
|---|---|
| `valid.m3u8` | Well-formed VOD media playlist (3 segments incl. a `DATERANGE`) — the "everything is fine" baseline. Zero findings from `media_doctor::check_playlist`/`check_hls_playlist`; parses cleanly here. |
| `bad-extinf.m3u8` | `EXTINF:15.0` segment against `EXT-X-TARGETDURATION:10` — a *semantic* rule violation (segment longer than its declared target duration) that `media-doctor`'s rule engine flags (`hls-extinf-exceeds-target`). Syntactically well-formed, so it parses cleanly under this crate's structural parser (which does not enforce that semantic constraint) — see the note in `hls_fixture_corpus.rs`. |
| `malformed-daterange.m3u8` | `EXT-X-DATERANGE` missing its required `ID` attribute — another *semantic* violation media-doctor flags (`hls-malformed-daterange`). `EXT-X-DATERANGE` is an unrecognized tag to `transmux::hls::MediaPlaylist::parse` (preserved verbatim into `extra_tags`), so it still parses cleanly here; the missing-`ID` check lives in media-doctor, not this structural parser. |
| `missing-extm3u.m3u8` | First line is `#EXT-X-VERSION:3`, no `#EXTM3U` header — a genuine **syntax** error. The one hand-made fixture this corpus test expects to fail parsing (`Error::HlsParse { reason: "missing #EXTM3U header", .. }`). |
| `reference-vod.m3u8` | 10-segment VOD playlist **generated by this workspace's own** `transmux::MediaPlaylist::to_m3u8()` (not hand-typed) — a round-trip sanity fixture also consumed by `transmux/tests/hls_playlist.rs`. Parses cleanly. |

Fetch/extraction date: n/a (authored in-repo, predates this issue).
Licence: project licence (MIT OR Apache-2.0), same as all in-repo source.

---

## Tier 1 — spec test vectors (`fixtures/hls/spec/`)

Source: `ll-hls-runtime/docs/examples.md`, itself a verbatim transcription
(commit `a07124ae`, 2026-08-01, issue #867) of §9 "Playlist Examples" from
**draft-pantos-hls-rfc8216bis-22** (IETF Internet-Draft, 1 May 2026),
source lines 4608–5023 of `specs/ietf_draft_pantos_hls_rfc8216bis.txt`.
Extraction/commit date into this fixture tree: 2026-08-02.

Licence/redistribution: IETF Internet-Drafts are governed by
[BCP 78](https://www.rfc-editor.org/info/bcp78) — freely reproducible in
full without modification (which these are, modulo the transcription's own
documented whitespace normalization — see `examples.md` header). This is
example/illustrative text, not the operative spec text.

These are **authoritative**: written by the spec's own authors. If our
parser disagrees with one, our parser is wrong (subject to the two
documented exceptions below).

### What's committed vs excluded

| §9 subsection | Committed as | Kind | Parses? |
|---|---|---|---|
| 9.1 Simple Media Playlist | `9.1-simple-media-playlist.m3u8` | Media | ✅ |
| 9.2 Live Media Playlist Using HTTPS | `9.2-live-media-playlist-https.m3u8` | Media | ✅ |
| 9.3 Playlist with Encrypted Media Segments | `9.3-encrypted-media-segments.m3u8` | Media | ✅ |
| 9.4 Multivariant Playlist | `9.4-multivariant-playlist.m3u8` | Master | ✅ |
| 9.5 Multivariant Playlist with I-Frames | `9.5-multivariant-with-iframes.m3u8` | Master | ✅ |
| 9.6 Multivariant Playlist with Alternative Audio | `9.6-multivariant-alternative-audio.m3u8` (joined) + `.rfc-verbatim.txt` (as-printed) | Master | ✅ (joined form) |
| 9.7 Multivariant Playlist with Alternative Video | `9.7-multivariant-alternative-video.m3u8` (joined) + `.rfc-verbatim.txt` (as-printed) | Master | ✅ (joined form) |
| 9.8 Session Data in a Multivariant Playlist | **excluded** — see below | Fragment | n/a |
| 9.9 CHARACTERISTICS attribute value | **excluded** — see below | Fragment | n/a |
| 9.10 EXT-X-DATERANGE Carrying SCTE-35 Tags | `9.10-daterange-scte35.m3u8` (joined) + `.rfc-verbatim.txt` (as-printed) | Media | ❌ (see Findings) |
| 9.11 Low-Latency Playlist | `9.11-low-latency-playlist.m3u8` | Media | ❌ (see Findings) |
| 9.12 Content Steering Playlist and Manifest | `9.12-content-steering.m3u8` (joined) + `.rfc-verbatim.txt` (as-printed) + `9.12-content-steering-manifest.json` | Master + JSON | ✅ (m3u8 half) |
| 9.13 Content Steering Manifest with Pathway Clone | `9.13-content-steering-pathway-clone-manifest.json` | JSON only | n/a (no m3u8 half) |

**9.8 and 9.9 are deliberately NOT committed.** Both are explicit fragments
in the spec's own prose, not complete playlists:

- §9.8 — the spec says "In this example, only the EXT-X-SESSION-DATA is
  shown"; the excerpt has no `#EXTM3U` header at all.
- §9.9 — not even a tag, just an isolated `CHARACTERISTICS=".."` attribute
  value shown to illustrate that attribute's comma-separated grammar.

Neither begins with `#EXTM3U`, so neither is a playlist a parser could ever
be expected to accept — committing either as a `.m3u8` fixture would just
be an always-red test with no signal. (Their content is fully reproduced,
with this same reasoning, in `ll-hls-runtime/docs/examples.md` §9.8/§9.9.)

### Line-continuation joining (9.6, 9.7, 9.10, 9.12)

The spec uses a trailing `\` plus indentation purely as an editorial
line-wrap convention for the Internet-Draft's fixed page width — its own
preamble to §9 says so explicitly ("In some examples a '\' is used for
readability to indicate that the tag continues on the following line.").
A real HLS playlist has each tag on one physical line; a real parser would
see a backslash-terminated line as a broken tag, not a continuation.

Four examples carry this artifact: **9.6**, **9.7**, **9.10**, and **9.12**
(9.8, already excluded above, also has it but doesn't matter). For each:

- The committed `.m3u8` is the **joined** form: continuations resolved
  (trailing `\` removed, the next line's alignment-only leading whitespace
  stripped, and the two concatenated with no added separator — verified by
  hand against each attribute list / hex string so no token boundary changes).
  This is what `hls_fixture_corpus.rs` parses.
- A sibling `.rfc-verbatim.txt` — **not** `.m3u8`, so it is not picked up by
  the test's directory-completeness check or parsed by anything — preserves
  the backslash-continued form exactly as printed, so the editorial-vs-real
  distinction stays visible and auditable.

9.10 additionally contains the spec's own `...` elision lines (both a
standalone playlist line and mid-hex-string continuations in the SCTE-35
payloads) — those are preserved as-is in both the joined and verbatim forms;
they are a different artifact from the continuation backslashes (see
Findings below).

### JSON manifests (9.12, 9.13)

§9.12 and §9.13 each carry a Content Steering Manifest, which is **JSON, not
m3u8**. Committed as standalone `.json` files (`9.12-content-steering-manifest.json`,
`9.13-content-steering-pathway-clone-manifest.json`), validated as parseable
JSON (`python3 -m json.tool`) at commit time but not parsed by
`hls_fixture_corpus.rs` (that test is playlist-only; no Content Steering
Manifest parser exists in this crate yet). §9.13's prose "resulting URIs"
list (three example URIs after the JSON block, showing the effect of the
Pathway Clone) is documentation, not itself a fixture — not committed.

### Findings: which fixtures fail to parse, and why

`transmux/tests/hls_fixture_corpus.rs` parses all 10 committed spec `.m3u8`
files. **8 parse cleanly; 2 fail:**

- **`9.10-daterange-scte35.m3u8`** — `Error::HlsParse` at line 2, reason
  `"media segment URI with no preceding #EXTINF"`.
- **`9.11-low-latency-playlist.m3u8`** — same error shape, at line 3.

Root cause for both: the spec's own `...` elision marker (§9.10: "...Media
Segment declarations for 60s worth of media"; §9.11: elided
`#EXT-X-PART` tags "removed from earlier Parent Segments") is a bare line
that doesn't start with `#`, so the parser reads it as a Media Segment URI.
Since no `#EXTINF` precedes it, that's a hard parse error by the crate's own
(correct) rules.

**This is not a missing-tag gap for issue #872.** Every *tag* in both
examples that this crate doesn't structurally model — `EXT-X-KEY`,
`EXT-X-MEDIA`, `EXT-X-CONTENT-STEERING`, `EXT-X-DATERANGE` (incl. its
`SCTE35-OUT`/`SCTE35-IN` sub-fields) — is preserved verbatim
(either `MediaPlaylist::extra_tags` or `MasterPlaylist::extra_tags`),
never rejected. §9.10 and §9.11 fail purely
because they are intentionally-incomplete excerpts of a longer playlist, not
because of unimplemented tag support. No real encoder emits a literal `...`
segment URI, so this is not evidence of a parser defect either — it's an
artifact of committing spec prose byte-for-byte. Recorded here rather than
worked around, per the issue's instruction not to weaken the assertion, not
implement missing tags, and not delete the fixture.

**Issue #870 calibration note** (Apple `mediastreamvalidator` run over the 8
parseable fixtures, `media-doctor/tests/mediastreamvalidator_oracle.rs`):
three more editorial/topology artifacts of these being spec-prose excerpts,
not real streams, surfaced there and are documented (with exact match
strings) in that test file rather than here —
`9.6-multivariant-alternative-audio.m3u8` and
`9.7-multivariant-alternative-video.m3u8` both use `CODECS="..."` (the same
elision convention as 9.10/9.11's bare `...` line, reused inline as a
placeholder attribute value); `9.12-content-steering.m3u8` deliberately
defines two Pathways' audio groups with different members (`NAME="English"`
vs `NAME="ENGLISH"`), which is the entire point of per-Pathway Content
Steering but trips the validator's older, pathway-unaware "same TYPE ⇒ same
members" rule.

---

## Tier 1b — hand-built fixtures (`fixtures/hls/handbuilt/`)

Added by issue **#872** (the nine remaining RFC 8216bis §4.4 tags).
**Authored in-repo — NOT spec vectors, NOT captured from any real stream.**
They live in their own `handbuilt/` directory, and are named so they cannot
be mistaken for a §9 example, precisely because a fixture that *looks* like
a spec vector gets treated as ground truth by every later reader and test.

Why they exist: six of the nine tags #872 implemented
(`EXT-X-GAP`, `EXT-X-BITRATE`, `EXT-X-PLAYLIST-TYPE`, `EXT-X-START`,
`EXT-X-DEFINE`, `EXT-X-SESSION-KEY`) appear in **no** §9 example at all, and
the seventh (`EXT-X-SESSION-DATA`) appears only in §9.8 — an explicit
fragment with no `#EXTM3U` line, excluded from Tier 1 above. Without these,
those tags would have no fixture-level coverage. Content is derived from
each tag's confirmed attribute grammar in
`broadcast-hls/docs/playlist-tags.md` (the §4.4 transcription), not invented
freehand.

Extraction/authoring date: 2026-08-02.
Licence: project licence (MIT OR Apache-2.0), same as all in-repo source.

| File | Kind | Tags exercised | Parses? |
|---|---|---|---|
| `multivariant-header-tags.m3u8` | Master | `EXT-X-INDEPENDENT-SEGMENTS`, `EXT-X-START` (negative `TIME-OFFSET` + `PRECISE=YES`), `EXT-X-DEFINE` (`NAME`/`VALUE` and `QUERYPARAM` forms), `EXT-X-SESSION-KEY` (`AES-128` with `KEYFORMAT`/`KEYFORMATVERSIONS`, and `SAMPLE-AES-CTR` with a 128-bit `IV`) | ✅ |
| `session-data-multivariant.m3u8` | Master | `EXT-X-SESSION-DATA` — the `URI` form, plus two `VALUE`+`LANGUAGE` entries sharing one `DATA-ID` (the en/es pair) | ✅ |
| `live-vod-gap-bitrate.m3u8` | Media | `EXT-X-PLAYLIST-TYPE:VOD`, `EXT-X-GAP`, `EXT-X-BITRATE` (carry-forward across a segment, then a change), `EXT-X-START` (positive offset, `PRECISE` absent), `EXT-X-DEFINE:IMPORT` + `{$base}` variable substitution, `EXT-X-INDEPENDENT-SEGMENTS` | ✅ |
| `daterange-scte35-media.m3u8` | Media | `EXT-X-DATERANGE` with `SCTE35-OUT` and `SCTE35-IN` (unmodeled tags, preserved in `extra_tags`) — derived from §9.10's abridged example by supplying the Media Segments the spec elides with its `...` markers | ✅ |
| `low-latency-parts-preload-report.m3u8` | Media | `EXT-X-PART`, `EXT-X-PRELOAD-HINT`, `EXT-X-RENDITION-REPORT`, `EXT-X-DISCONTINUITY`, `EXT-X-MAP`, `EXT-X-SERVER-CONTROL`, `EXT-X-PART-INF` — derived from §9.11's abridged example by replacing its leading `...` with a real LL-HLS header block | ✅ |

### Relationship to §9.8 (`session-data-multivariant.m3u8`)

The three `EXT-X-SESSION-DATA` lines are taken from **§9.8's fragment**, with
its editorial backslash line-continuations joined (same treatment as the
Tier 1 joined forms). It is **not verbatim §9.8** and must not be cited as
such: §9.8 is only those three tag lines — the spec's own prose says "In
this example, only the EXT-X-SESSION-DATA is shown" — so to make a *parseable
playlist* this fixture adds an `#EXTM3U` header and one
`EXT-X-STREAM-INF` + URI pair that appear nowhere in §9.8. That addition is
the entire reason it lives here in `handbuilt/` rather than in `spec/`, and
the reason §9.8 itself stays excluded from Tier 1.

### `EXT-X-VERSION` derivation (§8)

Version numbers here are **derived from the §8 table** in
`broadcast-hls/docs/version-compatibility.md`, not guessed. That doc's own
note explains why guessing high is harmful and not merely untidy: a client
that doesn't support a falsely-declared version is required by §8 to refuse
playback entirely.

**This table is executable, not prose.**
`broadcast-hls/tests/spec_fixture_version.rs` asserts `computed_version()`
(issue #880's §8 implementation) equals the value claimed below for each
fixture, *and* that each fixture's own declared `#EXT-X-VERSION` line
matches it — so a claim here cannot rot, and a fixture cannot drift from its
stated derivation. It doubles as the integration check between #872 and
#880: several §8 rows read tags that #872 gave typed representations, and a
row still string-matching `extra_tags` after that change would silently stop
firing and fail here.

| File | Declared | Why |
|---|---|---|
| `multivariant-header-tags.m3u8` | `11` | §8 row 11 — "contains an `EXT-X-DEFINE` tag with a `QUERYPARAM` attribute". This is the highest trigger present; the `NAME`/`VALUE` `EXT-X-DEFINE` alone would be row 8, and the `IV`/`KEYFORMAT` attributes rows 2/5. |
| `session-data-multivariant.m3u8` | *(none)* | **No §8 row is triggered.** `EXT-X-SESSION-DATA` has no version requirement, so no `EXT-X-VERSION` tag is emitted at all — matching §9.4, the spec's own multivariant example, which likewise carries none. (Version 7 would be wrong here: that row is triggered by `SERVICE` values for `INSTREAM-ID`, which this fixture does not contain.) |
| `live-vod-gap-bitrate.m3u8` | `8` | §8 row 8 — "contains variable substitution". The fixture both declares (`EXT-X-DEFINE:IMPORT="base"`) *and* substitutes (`{$base}/seg0.ts`), so the trigger is unambiguous. Floating-point `EXTINF` (row 3) is also present but lower. `EXT-X-GAP`/`EXT-X-BITRATE`/`EXT-X-PLAYLIST-TYPE`/`EXT-X-START` trigger no row. |
| `daterange-scte35-media.m3u8` | `3` | §8 row 3 — "floating-point `EXTINF` duration values" (the segments' 10.000 and 5.993 durations are fractional). No other row is triggered — `EXT-X-DATERANGE`/`SCTE35-*` attributes carry no version requirement. |
| `low-latency-parts-preload-report.m3u8` | `6` | §8 row 6 — "contains an `EXT-X-MAP` tag in a Media Playlist that does not contain `EXT-X-I-FRAMES-ONLY`". Row 3 (floating-point `EXTINF`) is also present but lower. `EXT-X-PART`/`EXT-X-PRELOAD-HINT`/`EXT-X-RENDITION-REPORT`/`EXT-X-SERVER-CONTROL` trigger no row. |

### Known semantic caveat

`live-vod-gap-bitrate.m3u8` uses `EXT-X-DEFINE:IMPORT`, which per §4.4.2.3
is only legal in a Media Playlist **loaded from** a Multivariant Playlist
(and a parser "MUST fail" if it wasn't). As a standalone committed file it
has no parent, so that cross-file precondition is deliberately unsatisfied —
`broadcast-hls` documents that it does not enforce cross-file
MUST-constraints (see the crate's README, "Round-trip fidelity"), which is
exactly what makes this fixture parseable. It is testing the tag's *syntax*,
not its resolution semantics. `IMPORT` is nonetheless the only form that
belongs in a Media Playlist, so there is nowhere else to cover it.

### Relationship to §9.10 (`daterange-scte35-media.m3u8`)

This fixture is **derived from §9.10's abridged example**, not verbatim
from it. §9.10 in the RFC uses a bare `...` on line 2 and the prose line
"... Media Segment declarations for 60s worth of media" to elide all the
Media Segments its DATERANGE tags are embedded in. Neither is valid playlist
syntax, so §9.10 itself cannot parse (see Tier 1 Findings). This fixture
supplies the three Media Segments the RFC elided — one before the out-point,
one spanning the splice, and one after the in-point — to make the tag
combination of `EXT-X-DATERANGE` + `SCTE35-OUT`/`SCTE35-IN` parseable as a
complete playlist. The two SCTE-35 hex payloads are the same as §9.10's,
joined from their editorial backslash continuations (same treatment as the
Tier 1 joined forms). The DATERANGE lines are unmodeled, so they land in
`extra_tags`.

### Relationship to §9.11 (`low-latency-parts-preload-report.m3u8`)

This fixture is **derived from §9.11's abridged example**. The RFC uses a
leading `...` on line 3 to stand in for "EXT-X-PART tags have been removed
from earlier Parent Segments" and the `EXT-X-VERSION`/`EXT-X-SERVER-CONTROL`/
`EXT-X-PART-INF`/`EXT-X-MAP` header block that precedes them. This fixture
replaces that `...` with the real header tags, making the playlist parseable
from its first line. The remaining lines — segments 268 through 272 (with
their PART tags), the DISCONTINUITY, the mid-roll segments 273/274 with
parts, preload-hint, and rendition-report — are exactly as in the RFC
(modulo variant URI names, which §9.11's intro says are illustrative and
not prescriptive). Unlike the DATERANGE fixture, all tags here are modeled
as typed struct fields (EXT-X-PART → `PartSpec`, EXT-X-PRELOAD-HINT →
`LowLatencyConfig::preload_hint_part`, EXT-X-RENDITION-REPORT →
`MediaPlaylist::rendition_reports`).

---

## Tier 2 — real streams (`fixtures/hls/real/`)

Source: Apple's public HLS example streams, linked from
<https://developer.apple.com/streaming/examples/>. Playlists only —
**no segment media (`.ts`/`.mp4`/`.aac`) committed**, per the issue's
instruction to keep this tree free of multi-megabyte binaries.

Fetch date: 2026-08-02. Fetched with `curl -s -L <url>`; all six returned
HTTP 200 at fetch time.

Licence/redistribution: Apple publishes these specifically as public
"HTTP Live Streaming (HLS) examples" for developers to test against
(the linked page is itself titled "Examples - HTTP Live Streaming"); no
authentication or signed URL was required. Treated as freely fetchable
reference material for interoperability testing, not redistributed as a
product — same posture as the RFC examples above.

### `real/bipbop-ts/` — classic MPEG-2 TS-segmented BipBop

Source master: `https://devstreaming-cdn.apple.com/videos/streaming/examples/bipbop_16x9/bipbop_16x9_variant.m3u8`.
The current `developer.apple.com/streaming/examples/` index no longer links
this classic stream directly (it now only links the newer `advanced-stream-*`
pages); this is the long-standing, widely-documented Apple "BipBop 16x9" TS
example URL from `devstreaming-cdn.apple.com` (the same CDN host serving the
still-linked advanced examples). Reachability was verified directly with
`curl -s -o /dev/null -w '%{http_code}'` (HTTP 200) on the fetch date rather
than assumed from memory.

| File | Source URL | For |
|---|---|---|
| `master.m3u8` | `.../bipbop_16x9/bipbop_16x9_variant.m3u8` | Multivariant playlist: 6 video/audio-muxed variants (`gear0`–`gear5`) + `EXT-X-I-FRAME-STREAM-INF` per gear, an `AUDIO="bipbop_audio"` group (one in-stream-default rendition with no `URI`, one alternate `URI`-addressed rendition), and a 12-language `SUBTITLES="subs"` group. The alternate-audio + multivariant real-stream evidence the AC calls for. |
| `gear1-video.m3u8` | `.../bipbop_16x9/gear1/prog_index.m3u8` | Media playlist for the lowest-bitrate video variant: `#EXT-X-BYTERANGE` segments all pointing into one `main.ts` — the real **MPEG-2 TS** shape (`#EXT-X-VERSION:4`, no `#EXT-X-MAP`, `.ts` byte-ranges). |
| `alternate-audio.m3u8` | `.../bipbop_16x9/alternate_audio_aac/prog_index.m3u8` | Media playlist for the alternate (non-default, `URI`-addressed) audio rendition — byte-ranges into `main.aac`. |

### `real/bipbop-fmp4-hevc/` — fMP4/CMAF-shaped BipBop (AVC+HEVC advanced example)

Source master: `https://devstreaming-cdn.apple.com/videos/streaming/examples/bipbop_adv_example_hevc/master.m3u8`
(linked from `advanced-stream-hevc.html`, Apple's "BipBop (AVC and HEVC)"
advanced-stream example).

| File | Source URL | For |
|---|---|---|
| `master.m3u8` | `.../bipbop_adv_example_hevc/master.m3u8` | Multivariant playlist: AVC and HEVC ladders × three audio codecs (AAC/AC-3/EC-3) each in its own `EXT-X-MEDIA` `AUDIO` group (`a1`/`a2`/`a3`), `TYPE=CLOSED-CAPTIONS` and `TYPE=SUBTITLES` groups, and `EXT-X-I-FRAME-STREAM-INF` trick-play renditions for both codecs. The fMP4 + multivariant-with-alternate-audio real-stream evidence the AC calls for. |
| `v5-video.m3u8` | `.../bipbop_adv_example_hevc/v5/prog_index.m3u8` | Media playlist for one AVC video variant: `#EXT-X-MAP:URI="main.mp4",BYTERANGE=...` init segment + `#EXT-X-BYTERANGE` media segments all into `main.mp4` — the real **fMP4/CMAF** shape. |
| `a1-audio.m3u8` | `.../bipbop_adv_example_hevc/a1/prog_index.m3u8` | Media playlist for the `a1` (AAC, default) alternate-audio rendition — same fMP4 byte-range-into-`main.mp4` shape. |

All six real-stream files above parse cleanly (`MasterPlaylist::parse` /
`MediaPlaylist::parse`, no errors) — see `hls_fixture_corpus.rs`.

No fetch failures to record for this run — every URL above resolved on the
first attempt; nothing was substituted or fabricated.

### Finding: these fixtures caught a real precision bug (issue #872)

When issue #872 added the round-trip assertion (`parse -> to_m3u8() ->
parse` must yield an equal document) to `hls_fixture_corpus.rs`,
`real/bipbop-ts/gear1-video.m3u8` **failed** — and it was the only tier that
did. Root cause: `to_m3u8()` rendered `#EXTINF` with a hardcoded `{:.3}`
and every other seconds value via integer-millisecond math, so any duration
finer than 1 ms was silently rounded on output. This playlist's real
durations are `9.9766`/`9.9433`, which came back as `9.977`/`9.943` — a
corrupted timeline on every repackage.

Fixed in `broadcast-hls` by rendering losslessly (keeping the compact
historical form whenever it re-parses bit-exactly, else the shortest
exactly-round-tripping decimal); regression-pinned by
`sub_millisecond_durations_survive_rendering` and
`round_trip_rfc_9_11_sub_millisecond_part_durations` in `src/lib.rs`.

Worth recording as the concrete payoff of Tier 2: **no hand-made fixture in
this repo could have caught it**, because every one of them was authored
with 3-decimal durations — the exact precision the buggy renderer happened
to preserve. The same class of bug also affected `EXT-X-PART:DURATION`
(RFC 8216bis §9.11's real values are `2.00004`/`4.00008`, which rendered as
`2`/`4`), which the spec tier could not catch either since §9.11 is one of
the two unparsable elision fixtures.

---

## Tier 3 — generated, round-trippable fixtures

**Deliberately deferred** (per the issue: "Skip for this story — generated
fixtures depend on implementation work not yet done"). The plan (segmenting
an existing repo TS capture through `transmux` into both TS-HLS and
CMAF-HLS output) is unchanged from the parent epic #866; revisit once that
segmenting path exists. Not committed here; nothing to list yet.
