name: Release dvb-ule

# Publishes the dvb-ule crate to crates.io on a `dvb-ule-v*` tag.
# dvb-ule is versioned and released INDEPENDENTLY of the lockstep `v*` tag
# (it is a standalone reusable crate with its own cadence), so it has its own
# publish workflow. CI is the only source of published artifacts — never
# `cargo publish` from a workstation. Requires the CARGO_REGISTRY_TOKEN secret.

on:
  push:
    tags: ["dvb-ule-v*"]

env:
  CARGO_TERM_COLOR: always

jobs:
  publish:
    name: publish dvb-ule to crates.io
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: stable
          targets: thumbv7em-none-eabi
      - uses: Swatinem/rust-cache@v2

      - name: Gate — dvb-ule tests
        run: cargo test -p dvb-ule --all-features --locked

      - name: Gate — dvb-ule clippy
        run: cargo clippy -p dvb-ule --all-features --all-targets --locked -- -D warnings

      - name: Gate — dvb-ule no_std build
        run: cargo build -p dvb-ule --no-default-features --target thumbv7em-none-eabi --locked

      - name: Gate — tag matches dvb-ule version
        run: |
          TAG="${GITHUB_REF_NAME#dvb-ule-v}"
          V=$(cargo metadata --no-deps --format-version 1 --locked \
              | python3 -c "import json,sys; print({p['name']: p['version'] for p in json.load(sys.stdin)['packages']}['dvb-ule'])")
          if [ "$V" != "$TAG" ]; then
            echo "::error::dvb-ule is $V but tag is $TAG"; exit 1
          fi

      # dvb-ule's dvb-common dependency must already be on crates.io.
      # Idempotent: an "already uploaded" error is treated as success.
      - name: Publish dvb-ule
        run: |
          if out=$(cargo publish -p dvb-ule --locked 2>&1); then
            echo "$out"; echo "::notice::published dvb-ule"
          elif echo "$out" | grep -qiE "already (been )?uploaded|already exists"; then
            echo "$out"; echo "::notice::dvb-ule already on crates.io at this version — skipping"
          else
            echo "$out"; echo "::error::failed to publish dvb-ule"; exit 1
          fi
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
