# Changelog — dvb-ule (deprecated shim)

## 0.1.1 — 2026-06-27

Deprecated shim crate created. The crate was renamed to `ule`; this
shim re-exports everything from `ule = "0.1"` for backwards
compatibility. Update your `Cargo.toml` to use `ule` directly.
