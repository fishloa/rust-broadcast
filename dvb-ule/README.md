# dvb-ule (deprecated)

This crate has been renamed to [`ule`](https://crates.io/crates/ule).

Update your `Cargo.toml`:

```toml
# before
dvb-ule = "0.1"

# after
ule = "0.1"
```

This shim re-exports everything from `ule` and will be removed in a
future release.
