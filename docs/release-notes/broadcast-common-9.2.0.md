# broadcast-common 9.2.0

Released 2026-08-02.

### Added

- `cenc::CencScheme` — the container-independent ISO/IEC 23001-7 scheme
  identity (`cenc`/`cens`/`cbc1`/`cbcs`) defined once below both `transmux`'s
  crypto boxes and `broadcast-hls`'s `#EXT-X-KEY` (issue #878 extraction).
