# hls-runtime 0.4.0

Released 2026-08-02.

### Changed

- Requires `transmux` 0.22 and `broadcast-hls` 0.1 (HLS playlist syntax
  extraction, issue #878).
- `HlsOrigin` renders playlists via `broadcast-hls` types.
