# hls-runtime 0.5.0

**Release date:** 2026-08-05

Epoch-pure dependency bumps for `transmux` 0.23, `media-plane` 0.3, and `broadcast-hls` 0.1. No functional or API change in this crate itself.

## What changed

- Requires `transmux` 0.23, `media-plane` 0.3, `broadcast-hls` 0.1.

## Migration

Requires the listed dependency versions. No API changes in this crate.
