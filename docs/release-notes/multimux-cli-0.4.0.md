# multimux-cli 0.4.0

**Release date:** 2026-08-02

Tracks multimux 0.6.0 — the admin API / signed-URL / TS-HLS release. No CLI-surface changes; the version bump reflects the breaking dependency.

## What changed

- Requires `multimux` 0.6.

## Migration

Requires `multimux` 0.6. No CLI flag or config-file changes.
