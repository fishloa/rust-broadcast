# multimux-cli 0.5.0

**Release date:** 2026-08-05

Tracks multimux 0.7.0 — MPTS, mid-stream tracks, Smooth output, DVR. No CLI-surface changes; the version bump reflects the breaking dependency.

## What changed

- Requires `multimux` 0.7.

## Migration

Requires `multimux` 0.7. No CLI flag or config-file changes.
