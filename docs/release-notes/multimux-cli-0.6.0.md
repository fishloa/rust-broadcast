# multimux-cli 0.6.0

Released 2026-08-07.

### Added

- `--srt-push <URL>`, `--rtmp-push <URL>`, `--rtsp-push <URL>` flags for
  relaying ingested media to downstream servers (#744).

### Changed

- Requires `multimux` 0.8 (push re-egress outputs).
