# timed-metadata 0.4.2

**Release date:** 2026-07-30

Removes the duplicate `ssai_ad_stitch` example that 0.4.1's move from `transmux` left as a byte-identical copy in both crates (cargo output-filename collision). Adds a `non_exhaustive_coverage.rs` drift guard (#806). No public API or behaviour change.

## Migration

No breaking changes.
