//! SCTE-35 ↔ HLS `EXT-X-DATERANGE` conversion — stub (Task 6).
use crate::error::Result;

/// Convert a `splice_info_section` to an `EXT-X-DATERANGE` attribute string.
/// (Stub — implemented in Task 6.)
pub fn scte35_to_daterange(_splice_raw: &[u8]) -> Result<alloc::string::String> {
    unimplemented!("Task 6")
}
