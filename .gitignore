target/
**/*.rs.bk
.DS_Store
# Paid ISO standards: consulted locally for hand transcription, never vendored.
specs/iso_iec_*.pdf
# Non-redistributable industry specs (NorDig, CI Plus): freely downloadable but
# their notices forbid reproduction — consulted locally, only transcriptions committed.
specs/nordig_*.pdf
specs/ci_plus_*.pdf
tools/dvb-si-audit/.venv/
tools/dvb-si-audit/out/
.worktree/
.worktrees/
.delegate/
.worktrees/

# stray cross-compile artifacts (never commit)
*.rlib
*.rmeta

# fetch-on-demand broadcast captures (tools/fetch-test-streams.sh) — never committed
/.test-streams/

# wasm-pack build output for the browser demo (generated; not committed)
/demo/pkg/
/demo/target/
.playwright-mcp/
# all scratch cargo target dirs (target/, target-gate/, target-ci/, target-rel/, …)
/target*/
target-*/
# CTA standards: free to download but copyright CTA — consulted locally, only transcriptions committed (like ci_plus/nordig).
specs/cta_*.pdf
